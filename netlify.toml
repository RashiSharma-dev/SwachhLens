[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — React Router ke liye zaruri
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
